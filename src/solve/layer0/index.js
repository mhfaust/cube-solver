// const { faceNames } = require('../../constants')

// const faceHaving = (predicate, cube) => {
//     return faceNames.find(faceName => predicate(cube[faceName]))
// }

// module.exports = {
//     faceHaving
// }